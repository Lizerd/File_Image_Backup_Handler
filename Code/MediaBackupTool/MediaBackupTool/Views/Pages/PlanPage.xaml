<UserControl x:Class="MediaBackupTool.Views.Pages.PlanPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Plan" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Generate Plan" Command="{Binding GeneratePlanCommand}" Padding="20,10" Margin="0,0,10,0"/>
            <TextBlock VerticalAlignment="Center">
                <Run Text="Total: "/>
                <Run Text="{Binding TotalUniqueFiles, Mode=OneWay}" FontWeight="SemiBold"/>
                <Run Text=" unique files, "/>
                <Run Text="{Binding TotalDuplicates, Mode=OneWay}" FontWeight="SemiBold"/>
                <Run Text=" duplicates"/>
            </TextBlock>
        </StackPanel>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="350"/>
            </Grid.ColumnDefinitions>

            <!-- Folder Tree -->
            <GroupBox Header="Destination Folders" Margin="0,0,10,0">
                <TreeView ItemsSource="{Binding RootFolders}"
                          VirtualizingStackPanel.IsVirtualizing="True"
                          VirtualizingStackPanel.VirtualizationMode="Recycling">
                    <TreeView.ItemTemplate>
                        <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                            <StackPanel Orientation="Horizontal">
                                <CheckBox IsChecked="{Binding CopyEnabled}" Margin="0,0,5,0"/>
                                <TextBlock Text="{Binding DisplayName}"/>
                                <TextBlock Text="{Binding UniqueCount, StringFormat=' ({0})'}" Foreground="Gray"/>
                            </StackPanel>
                        </HierarchicalDataTemplate>
                    </TreeView.ItemTemplate>
                </TreeView>
            </GroupBox>

            <!-- Details Panel -->
            <GroupBox Grid.Column="1" Header="Details">
                <StackPanel Margin="10">
                    <TextBlock Text="Select a folder to see details" Foreground="Gray"
                               Visibility="{Binding SelectedFolder, Converter={StaticResource NullToVisibility}, ConverterParameter=Invert}"/>

                    <StackPanel Visibility="{Binding SelectedFolder, Converter={StaticResource NullToVisibility}}">
                        <TextBlock Text="{Binding SelectedFolder.DisplayName}" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>
                        <TextBlock Text="{Binding SelectedFolder.WhyExplanation}" TextWrapping="Wrap" Foreground="Gray" Margin="0,0,0,15"/>

                        <TextBlock Text="Files in this folder:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <ListBox ItemsSource="{Binding FilesInFolder}" Height="300">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="5">
                                        <TextBlock Text="{Binding FileName}" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding SourcePath}" FontSize="11" Foreground="Gray" TextTrimming="CharacterEllipsis"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </StackPanel>
                </StackPanel>
            </GroupBox>
        </Grid>
    </Grid>
</UserControl>
