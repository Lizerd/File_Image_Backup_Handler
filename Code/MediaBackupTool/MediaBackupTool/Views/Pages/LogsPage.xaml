<UserControl x:Class="MediaBackupTool.Views.Pages.LogsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Logs &amp; Diagnostics" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
            <Button Content="Open Log Folder" Command="{Binding OpenLogFolderCommand}" Padding="15,8" Margin="0,0,10,0"/>
            <Button Content="Copy Summary to Clipboard" Command="{Binding CopySummaryToClipboardCommand}" Padding="15,8" Margin="0,0,10,0"/>
            <Button Content="Refresh" Command="{Binding RefreshLogsCommand}" Padding="15,8"/>
        </StackPanel>

        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Margin="0,0,0,10">
                <TextBlock>
                    <Run Text="Debug Log: " FontWeight="SemiBold"/>
                    <Run Text="{Binding DebugLogPath, Mode=OneWay}"/>
                </TextBlock>
                <TextBlock>
                    <Run Text="Error Log: " FontWeight="SemiBold"/>
                    <Run Text="{Binding ErrorLogPath, Mode=OneWay}"/>
                </TextBlock>
            </StackPanel>

            <GroupBox Grid.Row="1" Header="Recent Warnings &amp; Errors">
                <ListBox ItemsSource="{Binding RecentErrors}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Message}" FontFamily="Consolas" FontSize="11" TextWrapping="Wrap"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </GroupBox>
        </Grid>
    </Grid>
</UserControl>
