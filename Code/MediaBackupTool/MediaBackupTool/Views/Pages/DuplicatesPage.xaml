<UserControl x:Class="MediaBackupTool.Views.Pages.DuplicatesPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Duplicates" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,10,0"/>
            <TextBlock Grid.Column="1" VerticalAlignment="Center">
                <Run Text="{Binding TotalGroups, Mode=OneWay}" FontWeight="SemiBold"/>
                <Run Text=" duplicate groups, "/>
                <Run Text="{Binding TotalDuplicates, Mode=OneWay}" FontWeight="SemiBold"/>
                <Run Text=" total duplicates"/>
            </TextBlock>
        </Grid>

        <ListBox Grid.Row="2" ItemsSource="{Binding DuplicateGroups}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Expander>
                        <Expander.Header>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding HashHex}" FontFamily="Consolas" Width="150"/>
                                <TextBlock Text="{Binding OccurrenceCount, StringFormat='{}{0} occurrences'}" Margin="10,0"/>
                                <TextBlock Text="{Binding SizeBytes, Converter={StaticResource FileSizeConverter}}" Foreground="Gray"/>
                            </StackPanel>
                        </Expander.Header>
                        <ItemsControl ItemsSource="{Binding Paths}" Margin="30,5,0,5">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}" FontSize="11" Margin="0,2"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Expander>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
